# Copy to `.env` and fill in.
# `.env` is git-ignored.

# Weights & Biases project name. Defaults to "bus-passenger-count" if unset.
WANDB_PROJECT=bus-passenger-count
# Team workspace in wandb
WANDB_ENTITY=IA901-2026S1-bus-passenger-count
